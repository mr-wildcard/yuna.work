---
interface Props {
  text: string;
  class?: string;
}

const { text, class: cssClasses } = Astro.props;
---

<span
  set:html={text}
  class:list={[
    "text-tag-label-highlight",
    "bg-neutral3",
    "py-[3px]",
    "px-2.5",
    "rounded-[10px]",
    cssClasses,
  ]}
/>
