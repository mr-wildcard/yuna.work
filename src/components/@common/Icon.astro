---
import { Icon as AstroIcon } from "astro-icon/components";

const { name, width, height, ...props } = Astro.props;
---

<AstroIcon
  aria-hidden="true"
  width={width}
  height={height}
  name={name}
  {...props}
/>
